const n = -10

const PNZ = (n) => {
    if (n > 0)
        console.log(n + " is positive")

    else if (n < 0)
        console.log(n + " is negative")

    else
        console.log(n + " is zero")
}

PNZ(n)